# Backend jobs (Railway)

Placeholder for scheduled/cron-style work:

- **Scrape** — fetch articles from configured sources.
- **Generate** — build newsletter content (e.g. with an AI provider).
- **Send** — send emails to users at their preferred time (e.g. cron or queue).

Use Railway cron or an external scheduler to hit protected endpoints, or run these as worker processes.
